// package main

// import "fmt"

// func main() {

	// ******************************
	// numbers := []int{1, 2, 3}

	// elm := 0

	// result := make([]int, len(numbers)+1)

	// result[0] = elm

	// copy(result[1:], numbers)

	// fmt.Println(result)
	//********************************
	// numbers := []int{1, 2, 3}
	// newSlice := []int{5, 6}

	// result := append(numbers, newSlice...)
	// fmt.Println(result)
	// ******************************

	// numbers = append(numbers, 5, 7, 9, 3, 2)

	// numbers := make([]int, 3, 5)

	// numbers = append(numbers, 6)
	// numbers = append(numbers, 5)
	// numbers = append(numbers, 4)
	// // numbers = append(numbers, 3)
	// // numbers = append(numbers, 6)

	// fmt.Println(numbers)
	// fmt.Println("length:", len(numbers))
	// fmt.Println("capacity:", cap(numbers))

}

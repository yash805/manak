package main

import "fmt"

func main() {

	// var name [5]string

	// name[0] = "sam"
	// name[1] = "john"

	// fmt.Println(name)

	// var numbers = [5]int{1, 2, 3, 4, 5}
	// fmt.Println(numbers)
	// fmt.Println(numbers[2])
	// fmt.Println(len(numbers))

	var price [5]string
	fmt.Println(price)
	fmt.Printf("price is %q\n", price)
}
